{% extends "_layout" %}

{% set featureImage = {
  mode: 'crop',
  width: 500,
  height: 300,
  quality: 90
} %}


{% set architectsQuery = craft.entries()
    .section('architect')
%}

{% set architects = architectsQuery.all() %}


{% block content %}

<div clas="container">
    <div class="row mb-2">
        <div class="col-12">
            <h1 class="text-4xl text-black font-display my-4">{{ entry.title }}</h1>
        </div>
    </div>

    <div class="row mb-2">

        <div class="d-flex align-items-start flex-row">
            {% for architect in architects %}
            <div class="architect me-3">
                {% if architect.mainImage|length %}
                {% set image = architect.mainImage.first() %}
                    <img src="{{ image.getUrl(featureImage) }}" alt="{{ image.title }}" />
                {% endif %}
                <h3><a href="{{ architect.url }}">{{ architect.title }}</a></h3>
            </div>
            {% endfor %}
        </div>

    </div>
</div>

{% endblock %}